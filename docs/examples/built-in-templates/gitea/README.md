# Gitea Built-in Template

Lightweight Git service for environment manifests and generated component repositories.

This template is intended for local and small PAAP environments where GitLab is too heavy.
It stores data on a PVC and uses Gitea's built-in SQLite database.
