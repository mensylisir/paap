# PAAP manages the namespace-local ServiceAccount and RBAC for this chart.
serviceAccount:
  create: false
persistence:
  enabled: false
metrics:
  enabled: true
  serviceMonitor:
    enabled: true
networkPolicy:
  enabled: false
resourcesPreset: "nano"
