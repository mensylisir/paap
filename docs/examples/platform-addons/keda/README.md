# KEDA

KEDA extends Kubernetes scaling with event-driven triggers such as queues, Kafka lag, Prometheus queries, and external metrics.

PAAP uses it for component ScaledObject delivery after an administrator enables the plugin.
