# kpack

kpack builds source components into OCI images with Cloud Native Buildpacks.

PAAP uses this plugin for source-based component delivery. It is not installed by default; administrators enable it when the cluster should support source-to-image workflows.
