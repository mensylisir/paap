# KubeVirt

KubeVirt enables VM-backed database and middleware service templates.

Enable it only on clusters with suitable virtualization, storage, networking, and security policy support. PAAP does not install it by default.
