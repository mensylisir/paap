# metrics-server

metrics-server provides the `metrics.k8s.io` API used by Kubernetes HPA and basic resource usage inspection.

PAAP treats it as a cluster-level platform plugin. Disabling it in PAAP removes the manifests installed from this package and marks the plugin disabled.
