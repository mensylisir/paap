{
  "key": "springboot-postgres-druid",
  "name": "Spring Boot + PostgreSQL + Druid",
  "version": "v1",
  "description": "需要 Druid 监控和连接池控制的 Spring Boot PostgreSQL 后端。",
  "framework": "springboot",
  "componentTypes": ["backend"],
  "sortOrder": 22,
  "nativeConfigs": [{ "name": "application.yml", "path": "files/application.yml" }],
  "files": [{ "name": "Spring Boot application.yml", "configMapName": "{{configMapName}}", "key": "application.yml", "recommendedMountPath": "/etc/paap/application.yml" }],
  "env": [
    { "name": "SPRING_CONFIG_ADDITIONAL_LOCATION", "source": "value", "value": "file:/etc/paap/application.yml" },
    { "name": "POSTGRES_PASSWORD", "source": "secret", "refName": "{{secretName}}", "refKey": "POSTGRES_PASSWORD" },
    { "name": "DRUID_ADMIN_PASSWORD", "source": "secret", "refName": "{{secretName}}", "refKey": "DRUID_ADMIN_PASSWORD" }
  ],
  "secrets": [
    { "name": "{{secretName}}", "data": { "POSTGRES_PASSWORD": "__TEMPLATE__POSTGRES_PASSWORD__PostgreSQL 密码__", "DRUID_ADMIN_PASSWORD": "__TEMPLATE__DRUID_ADMIN_PASSWORD__Druid 控制台密码__" } }
  ]
}
