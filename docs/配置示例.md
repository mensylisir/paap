### 1. Spring Boot 基础配置文件

**适用场景：** 无数据库的微服务、纯逻辑网关或工具类应用。

```
server:
  port: 8080
  shutdown: graceful
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      force: true

spring:
  application:
    name: base-service
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    default-property-inclusion: non_null

logging:
  level:
    root: INFO
    org.springframework.web: INFO
  file:
    name: logs/${spring.application.name}.log
  logback:
    rollingpolicy:
      max-file-size: 100MB
      max-history: 7
```

------



### 2. Spring Boot + Postgres (默认 Hikari 池)

**适用场景：** 标准 Spring Data JPA 项目。

```
server:
  port: 8080

spring:
  application:
    name: springboot-postgres-hikari

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/mydb?reWriteBatchedInserts=true
    username: pguser
    password: pgpassword
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 30000
      pool-name: HikariPool-Postgres
      max-lifetime: 1800000
      connection-timeout: 30000

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        jdbc:
          batch_size: 50

logging:
  level:
    root: INFO
    org.hibernate.SQL: DEBUG
```

------



### 3. Spring Boot + Postgres + Druid 连接池

**适用场景：** 需要数据库监控和高级防注入功能的项目。

```
server:
  port: 8080

spring:
  application:
    name: springboot-postgres-druid

  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      driver-class-name: org.postgresql.Driver
      url: jdbc:postgresql://localhost:5432/mydb
      username: pguser
      password: pgpassword
      initial-size: 5
      min-idle: 5
      max-active: 30
      max-wait: 60000
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      validation-query: SELECT 1
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      filters: stat,wall,slf4j
      stat-view-servlet:
        enabled: true
        url-pattern: /druid/*
        login-username: admin
        login-password: admin_password

logging:
  level:
    root: INFO
```

------



### 4. Spring Boot + Postgres 集群 + Druid

**适用场景：** 数据库主从架构或有负载均衡器的集群。

```
server:
  port: 8080

spring:
  application:
    name: springboot-postgres-cluster-druid

  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      # 连接两个节点，targetServerType=master 确保写入请求在主库
      url: jdbc:postgresql://192.168.1.10:5432,192.168.1.11:5432/mydb?targetServerType=master&loadBalanceHosts=true
      driver-class-name: org.postgresql.Driver
      username: pguser
      password: pgpassword
      initial-size: 10
      max-active: 50
      min-idle: 10
      # 集群环境下心跳检测尤为重要
      keep-alive: true
      validation-query: SELECT 1
      test-while-idle: true
      filters: stat,wall

logging:
  level:
    root: INFO
```

------



### 5. Spring Boot + Postgres + Redis 单实例

**适用场景：** 最常见的带缓存的 Web 应用。

```
server:
  port: 8080

spring:
  application:
    name: springboot-postgres-redis-single

  # 数据库
  datasource:
    url: jdbc:postgresql://localhost:5432/mydb
    username: pguser
    password: pgpassword
    driver-class-name: org.postgresql.Driver

  # Redis 单机
  redis:
    host: 127.0.0.1
    port: 6379
    password: redis_password
    database: 0
    timeout: 5000ms
    lettuce:
      pool:
        max-active: 20
        max-idle: 10
        min-idle: 2
        max-wait: 3000ms

logging:
  level:
    root: INFO
```

------



### 6. Spring Boot + Postgres + Redis 哨兵 (Sentinel)

**适用场景：** Redis 需要高可用切换的场景。

```
server:
  port: 8080

spring:
  application:
    name: springboot-postgres-redis-sentinel

  datasource:
    url: jdbc:postgresql://localhost:5432/mydb
    username: pguser
    password: pgpassword
    driver-class-name: org.postgresql.Driver

  # Redis 哨兵
  redis:
    sentinel:
      master: mymaster
      nodes: 192.168.1.101:26379,192.168.1.102:26379,192.168.1.103:26379
    password: redis_password
    database: 0
    lettuce:
      pool:
        max-active: 50
        max-idle: 10

logging:
  level:
    root: INFO
```

------



### 7. Spring Boot + Postgres + Redis 集群 (Cluster)

**适用场景：** 大规模分布式缓存场景。

```
server:
  port: 8080

spring:
  application:
    name: springboot-postgres-redis-cluster

  datasource:
    url: jdbc:postgresql://localhost:5432/mydb
    username: pguser
    password: pgpassword
    driver-class-name: org.postgresql.Driver

  # Redis 集群
  redis:
    cluster:
      nodes: 192.168.1.10:6379,192.168.1.11:6379,192.168.1.12:6379,192.168.1.13:6379
      max-redirects: 3
    password: redis_password
    lettuce:
      cluster:
        refresh:
          adaptive: true # 拓扑结构自适应刷新
          period: 60s
      pool:
        max-active: 100
        max-idle: 20

logging:
  level:
    root: INFO
```

------



### 8. Nginx 基础配置文件

**文件名：** nginx.conf

```
user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    sendfile on;
    keepalive_timeout 65;
    gzip on;

    server {
        listen 80;
        server_name localhost;

        location / {
            root /usr/share/nginx/html;
            index index.html index.htm;
            try_files $uri $uri/ /index.html;
        }
    }
}
```

------



### 9. Nginx default.conf（前端 SPA 常用）

**适用场景：** Vue/React/Vite 等前端静态站点容器，通常挂载到 `/etc/nginx/conf.d/default.conf`。

**文件名：** default.conf

```
server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html index.htm;

    gzip on;
    gzip_min_length 1k;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    location = /healthz {
        access_log off;
        add_header Content-Type text/plain;
        return 200 "ok\n";
    }

    location /assets/ {
        try_files $uri =404;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location /api/ {
        proxy_pass http://backend:8080/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 5s;
        proxy_read_timeout 60s;
    }

    location /admin-api/ {
        proxy_pass http://admin-backend:8080/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 5s;
        proxy_read_timeout 60s;
    }

    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache";
    }
}
```

------



### 10. Nginx + Upstream (后端负载均衡)

**文件名：** nginx.conf

```
worker_processes auto;

events {
    worker_connections 2048;
}

http {
    upstream backend_app {
        server 192.168.1.10:8080 weight=1;
        server 192.168.1.11:8080 weight=1;
        keepalive 32;
    }

    server {
        listen 80;
        server_name api.example.com;

        location / {
            proxy_pass http://backend_app;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

            # 代理超时配置
            proxy_connect_timeout 5s;
            proxy_send_timeout 60s;
            proxy_read_timeout 60s;
        }
    }
}
```

------



### 11. Gin + YAML 配置文件

**文件名：** config.yaml

```
server:
  port: 8888
  mode: "release"

database:
  driver: "postgres"
  dsn: "host=127.0.0.1 user=dbuser password=dbpass dbname=mydb port=5432 sslmode=disable"
  max_idle: 10
  max_open: 50

redis:
  addr: "127.0.0.1:6379"
  password: ""
  db: 0

log:
  level: "info"
  path: "./logs/app.log"
```

------



### 12. Gin + TOML 配置文件

**文件名：** config.toml

```
[server]
port = 8888
mode = "debug"

[database]
driver = "postgres"
dsn = "host=localhost user=user password=pass dbname=test port=5432"
max_idle = 10
max_open = 50

[redis]
addr = "127.0.0.1:6379"
password = ""
db = 0

[log]
level = "debug"
```

------



### 13. Gin + INI 配置文件

**文件名：** config.ini

```
[server]
port = 8888
mode = release

[database]
driver = postgres
dsn = host=127.0.0.1 user=root password=root dbname=gin_db port=5432 sslmode=disable
max_idle = 10
max_open = 100

[redis]
addr = 127.0.0.1:6379
password =
db = 0

[log]
level = info
```



#### 14. Spring Boot + Postgres + RabbitMQ (消息队列)

**适用场景：** 需要异步处理、削峰填谷的任务型应用。

```
server:
  port: 8080

spring:
  application:
    name: springboot-rabbitmq-service

  # 数据库配置
  datasource:
    url: jdbc:postgresql://localhost:5432/orders_db
    username: pguser
    password: pgpassword
    driver-class-name: org.postgresql.Driver

  # RabbitMQ 配置
  rabbitmq:
    host: 192.168.1.50
    port: 5672
    username: admin
    password: rabbit_password
    virtual-host: /my_vhost
    connection-timeout: 5000ms
    publisher-confirm-type: correlated # 开启发送确认
    publisher-returns: true           # 开启退回回调
    listener:
      simple:
        acknowledge-mode: manual      # 手动确认签收
        concurrency: 5                # 消费者线程池
        max-concurrency: 10
        prefetch: 1                   # 每次抓取一条，防止堆积

logging:
  level:
    root: INFO
```

#### 15. Spring Boot + Postgres + Nacos (注册与配置中心)

**适用场景：** 阿里系 Spring Cloud Alibaba 微服务架构。

```
server:
  port: 8080

spring:
  application:
    name: user-service

  cloud:
    nacos:
      # 服务注册发现
      discovery:
        server-addr: 192.168.1.100:8848
        namespace: dev-id
        group: DEFAULT_GROUP
      # 配置中心动态刷新
      config:
        server-addr: 192.168.1.100:8848
        file-extension: yaml
        namespace: dev-id
        group: DEFAULT_GROUP
        shared-configs:
          - data-id: common-db.yaml
            refresh: true

# 数据库等配置通常放在 Nacos 远程，本地只留 nacos 地址
```

------



###

#### 16. FastAPI + Postgres + Redis (生产级 YAML)

**适用场景：** 高性能异步接口开发。

```
# config.yaml
app:
  name: "fastapi-service"
  host: "0.0.0.0"
  port: 8000
  reload: false
  workers: 4

database:
  url: "postgresql+asyncpg://user:pass@localhost:5432/dbname"
  pool_size: 20
  max_overflow: 10

redis:
  host: "localhost"
  port: 6379
  db: 0
  password: "pwd"

security:
  secret_key: "your-secret-key-here"
  algorithm: "HS256"
  access_token_expire_minutes: 30
```

#### 17. Django + Postgres + Redis (Settings.py 结构)

**适用场景：** 传统重量级 Web 应用。

```
# settings.py 核心部分
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'mydb',
        'USER': 'pguser',
        'PASSWORD': 'pgpassword',
        'HOST': '127.0.0.1',
        'PORT': '5432',
        'CONN_MAX_AGE': 600, # 数据库长连接
    }
}

CACHES = {
    "default": {
        "BACKEND": "django_redis.cache.RedisCache",
        "LOCATION": "redis://127.0.0.1:6379/1",
        "OPTIONS": {
            "CLIENT_CLASS": "django_redis.client.DefaultClient",
            "PASSWORD": "redis_password",
            "CONNECTION_POOL_KWARGS": {"max_connections": 100}
        }
    }
}
```

------



###

#### 18. NestJS + Postgres + Redis (环境变量 .env 模式)

**适用场景：** TypeScript 后端。

```
# .env 文件
APP_PORT=3000
APP_ENV=production

# 数据库
DB_TYPE=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=admin
DB_PASSWORD=admin_password
DB_DATABASE=nest_db
DB_SYNCHRONIZE=false

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=null
REDIS_TTL=600

# JWT
JWT_SECRET=super_secret_key
JWT_EXPIRES_IN=7d
```

------



#### 19. Nginx + SSL/HTTPS + 强制跳转

**适用场景：** 生产环境门户安全配置。

```
server {
    listen 80;
    server_name example.com;
    return 301 https://$server_name$request_uri; # 强制 HTTP 转 HTTPS
}

server {
    listen 443 ssl;
    server_name example.com;

    ssl_certificate /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;

    # 现代加密套件优化
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256';
    ssl_prefer_server_ciphers on;

    location / {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
    }
}
```

#### 20. Nginx + 静态资源分离缓存

**适用场景：** 提升图片、JS、CSS 加载速度。

```
server {
    listen 80;

    # 静态文件不经过后端
    location ~* \.(jpg|jpeg|png|gif|ico|css|js)$ {
        root /var/www/static;
        expires 30d;           # 浏览器缓存30天
        add_header Cache-Control "public, no-transform";
        access_log off;        # 关闭静态资源日志，减少IO
    }

    # 动态请求转发
    location /api/ {
        proxy_pass http://backend_upstream;
    }
}
```

------



#### 21. Vue/React (Vite 模式)

**适用场景：** 前端不同环境地址配置。

```
# .env.production
VITE_APP_TITLE=企业级后台管理系统
VITE_API_BASE_URL=https://api.example.com
VITE_UPLOAD_URL=https://oss.example.com

# 开启压缩
VITE_BUILD_COMPRESS=gzip
VITE_DROP_CONSOLE=true
```
