---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: serviceinstances.paap.io
spec:
  group: paap.io
  names:
    kind: ServiceInstance
    listKind: ServiceInstanceList
    plural: serviceinstances
    singular: serviceinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: ServiceInstance is the Schema for the serviceinstances API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServiceInstanceSpec defines the desired state of ServiceInstance
            properties:
              clusterRole:
                description: 工具需要的集群级只读权限（例如 ArgoCD/Prometheus 的 discovery/cache）
                properties:
                  rules:
                    items:
                      properties:
                        apiGroups:
                          items:
                            type: string
                          type: array
                        resources:
                          items:
                            type: string
                          type: array
                        verbs:
                          items:
                            type: string
                          type: array
                      required:
                      - apiGroups
                      - resources
                      - verbs
                      type: object
                    type: array
                required:
                - rules
                type: object
              environmentRole:
                description: 工具需要对同环境内其它 namespace 的操作权限（工具/中间件/数据库等）
                properties:
                  rules:
                    items:
                      properties:
                        apiGroups:
                          items:
                            type: string
                          type: array
                        resources:
                          items:
                            type: string
                          type: array
                        verbs:
                          items:
                            type: string
                          type: array
                      required:
                      - apiGroups
                      - resources
                      - verbs
                      type: object
                    type: array
                required:
                - rules
                type: object
              environmentRef:
                description: 关联的环境（同 namespace 下的 Environment CR 名）
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              helm:
                description: Helm chart installation managed by the operator.
                properties:
                  chartArchivePath:
                    type: string
                  chartName:
                    type: string
                  chartRepo:
                    type: string
                  chartVersion:
                    type: string
                  namespace:
                    type: string
                  platformManifest:
                    type: string
                  presetValues:
                    type: string
                  releaseName:
                    type: string
                  s3Bucket:
                    type: string
                  s3Key:
                    type: string
                  values:
                    additionalProperties:
                      type: string
                    type: object
                required:
                - namespace
                - releaseName
                type: object
              manifestsRef:
                description: 渲染后的部署清单（存储在 ConfigMap 中）
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                - namespace
                type: object
              parameters:
                additionalProperties:
                  type: string
                description: 用户提交的参数
                type: object
              serviceAccount:
                description: SA 配置（自动生成，namespace 为 ToolNamespace）
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                - namespace
                type: object
              toolNamespace:
                description: 工具独占的 namespace（{app}-{env}-{tool}）
                type: string
              toolNamespaceRole:
                description: 工具在自己 namespace 内需要的权限（如访问 CRD）
                properties:
                  rules:
                    items:
                      properties:
                        apiGroups:
                          items:
                            type: string
                          type: array
                        resources:
                          items:
                            type: string
                          type: array
                        verbs:
                          items:
                            type: string
                          type: array
                      required:
                      - apiGroups
                      - resources
                      - verbs
                      type: object
                    type: array
                required:
                - rules
                type: object
              type:
                description: 服务类型：deploy / ci / monitor / postgresql / redis / ...
                type: string
              workloadRole:
                description: 工具需要对负载 namespace 的操作权限
                properties:
                  rules:
                    items:
                      properties:
                        apiGroups:
                          items:
                            type: string
                          type: array
                        resources:
                          items:
                            type: string
                          type: array
                        verbs:
                          items:
                            type: string
                          type: array
                      required:
                      - apiGroups
                      - resources
                      - verbs
                      type: object
                    type: array
                required:
                - rules
                type: object
            required:
            - environmentRef
            - serviceAccount
            - toolNamespace
            - type
            - workloadRole
            type: object
          status:
            description: ServiceInstanceStatus defines the observed state of ServiceInstance
            properties:
              components:
                description: 工具组件状态
                items:
                  properties:
                    kind:
                      type: string
                    message:
                      type: string
                    name:
                      type: string
                    ready:
                      type: boolean
                    reason:
                      type: string
                    replicas:
                      type: string
                  required:
                  - kind
                  - name
                  - ready
                  - replicas
                  type: object
                type: array
              conditions:
                description: 条件
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: 观察到的 generation
                format: int64
                type: integer
              phase:
                description: 'Phase: Pending / Installing / Running / Upgrading /
                  Deleting / Error'
                type: string
              rbacNamespaces:
                description: RBAC 覆盖的 namespace 列表
                items:
                  properties:
                    namespace:
                      type: string
                    roleBindingCreated:
                      type: boolean
                    roleCreated:
                      type: boolean
                  required:
                  - namespace
                  - roleBindingCreated
                  - roleCreated
                  type: object
                type: array
              serviceAccount:
                description: SA 状态
                properties:
                  created:
                    type: boolean
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - created
                - name
                - namespace
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
